---
apiVersion: v1
kind: Service
metadata:
  name: stilton
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: http
    targetPort: 80
    port: 80
  selector:
    app: cheese
    task: stilton
